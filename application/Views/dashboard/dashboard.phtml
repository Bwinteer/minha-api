<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $title ?? 'Dashboard' ?></title>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>📊 <?= $title ?></h1>
        </header>

        <?php if (!$success): ?>
            <div class="error-message">
                <h3>❌ Erro ao carregar os dados</h3>
                <p><?= htmlspecialchars($error ?? 'Erro desconhecido') ?></p>
            </div>
        <?php else: ?>
            
            <?php if ($resumo_geral): ?>
            <section class="resumo-cards">
                <div class="card">
                    <span class="card-icon">📁</span>
                    <div class="card-title">Total de Projetos</div>
                    <div class="card-value"><?= $resumo_geral['total_projetos'] ?? 0 ?></div>
                </div>
                <div class="card">
                    <span class="card-icon">📋</span>
                    <div class="card-title">Total de Tarefas</div>
                    <div class="card-value"><?= $resumo_geral['total_tarefas'] ?? 0 ?></div>
                </div>
                <div class="card">
                    <span class="card-icon">✅</span>
                    <div class="card-title">Tarefas Concluídas</div>
                    <div class="card-value"><?= $resumo_geral['total_concluidas'] ?? 0 ?></div>
                </div>
                <div class="card">
                    <span class="card-icon">⏳</span>
                    <div class="card-title">Tarefas Pendentes</div>
                    <div class="card-value"><?= $resumo_geral['total_pendentes'] ?? 0 ?></div>
                </div>
                <div class="card">
                    <span class="card-icon">📈</span>
                    <div class="card-title">Progresso Geral</div>
                    <div class="card-percentage"><?= $resumo_geral['percentual_geral'] ?? 0 ?>%</div>
                </div>
            </section>
            <?php endif; ?>

            <section class="projetos-section">
                <h2 class="section-title">
                    <span>🎯</span>
                    Detalhamento por Projeto
                </h2>

                <?php if (!empty($projetos)): ?>
                    <?php foreach ($projetos as $projeto): ?>
                        <article class="projeto-card">
                            <h3 class="projeto-nome"><?= htmlspecialchars($projeto['projeto_nome']) ?></h3>
                            <p class="projeto-descricao">
                                <?= htmlspecialchars($projeto['projeto_descricao'] ?: 'Sem descrição') ?>
                            </p>
                            
                            <div class="projeto-stats">
                                <div class="stat-item">
                                    <span class="stat-value"><?= $projeto['total_tarefas'] ?></span>
                                    <div class="stat-label">Total</div>
                                </div>
                                <div class="stat-item">
                                    <span class="stat-value" style="color: #4CAF50"><?= $projeto['tarefas_concluidas'] ?></span>
                                    <div class="stat-label">Concluídas</div>
                                </div>
                                <div class="stat-item">
                                    <span class="stat-value" style="color: #ff9800"><?= $projeto['tarefas_pendentes'] ?></span>
                                    <div class="stat-label">Pendentes</div>
                                </div>
                                <div class="stat-item">
                                    <span class="stat-value" style="color: #2196F3"><?= $projeto['percentual_conclusao'] ?>%</span>
                                    <div class="stat-label">Progresso</div>
                                </div>
                            </div>
                            
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: <?= $projeto['percentual_conclusao'] ?>%"></div>
                            </div>
                        </article>
                    <?php endforeach; ?>
                <?php else: ?>
                    <div class="no-data">
                        <p>📭 Nenhum projeto com tarefas encontrado.</p>
                        <p>Crie alguns projetos e tarefas para ver as estatísticas aqui!</p>
                    </div>
                <?php endif; ?>
            </section>

        <?php endif; ?>
        
        <footer class="footer">
            <small>Última atualização: <?= $timestamp ?? date('Y-m-d H:i:s') ?></small>
        </footer>
    </div>
    <style>
        :root {
            --primary-color: #bebfc5ff;
            --secondary-color: #764ba2;
            --card-bg: rgba(255, 255, 255, 0.95);
            --text-color-dark: #333;
            --text-color-medium: #666;
            --text-color-light: #f8f9fa;
            --success-color: #4CAF50;
            --pending-color: #ff9800;
            --progress-color: #2196F3;
            --bg-gradient: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            padding: 20px;
            color: var(--text-color-dark);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            color: white;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .header p {
            font-size: 1rem;
        }

        .resumo-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .card, .projetos-section {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
        }

        /* Ícones nos cards de resumo */
        .card-icon {
            font-size: 2rem; /* Reduzido de 2.5rem para 2rem */
            margin-bottom: 15px;
            display: block;
        }

        .card-title {
            font-size: 1.1rem;
            color: var(--text-color-medium);
            margin-bottom: 10px;
            font-weight: 600;
        }

        .card-value {
            font-size: 2.2rem;
            font-weight: bold;
            color: var(--text-color-dark);
        }
        
        .card-percentage {
            font-size: 1.4rem;
            color: var(--success-color);
            font-weight: bold;
        }

        .projetos-section {
            padding: 30px;
        }

        .section-title {
            font-size: 1.8rem;
            color: var(--text-color-dark);
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .section-title span {
            font-size: 1.2rem; /* Adicionado para controlar o tamanho do ícone */
        }

        .projeto-card {
            background: var(--text-color-light);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 4px solid var(--primary-color);
            transition: all 0.3s ease;
        }

        .projeto-card:hover {
            background: #e9ecef;
            transform: translateX(5px);
        }

        .projeto-nome {
            font-size: 1.3rem;
            font-weight: bold;
            color: var(--text-color-dark);
            margin-bottom: 5px;
        }

        .projeto-descricao {
            color: var(--text-color-medium);
            font-size: 0.95rem;
            margin-bottom: 15px;
        }

        .projeto-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 15px;
        }

        .stat-item {
            text-align: center;
            background: white;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .stat-value {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--text-color-dark);
            display: block;
        }

        .stat-label {
            font-size: 0.85rem;
            color: var(--text-color-medium);
            margin-top: 5px;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background-color: #e9ecef;
            border-radius: 4px;
            overflow: hidden;
            margin-top: 10px;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--success-color), #45a049);
            transition: width 0.5s ease;
        }
        
        .error-message {
            background: rgba(244, 67, 54, 0.1);
            color: #d32f2f;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            margin: 20px 0;
        }

        .no-data {
            text-align: center;
            color: var(--text-color-medium);
            padding: 40px;
            font-style: italic;
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            color: rgba(255,255,255,0.7);
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2rem;
            }
            
            .resumo-cards {
                grid-template-columns: 1fr;
            }
            
            .projeto-stats {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</body>
</html>